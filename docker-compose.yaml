version: "3.8"

services:
  gateway:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./routes.yaml:/root/routes.yaml
      - ./data:/root/data
    environment:
      - DB_PATH=/root/data/guardpost.db
      - GIN_MODE=release
